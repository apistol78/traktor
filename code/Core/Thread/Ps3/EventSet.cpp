#include <cassert>
#include "Core/Thread/EventSet.h"

namespace traktor
{
	
void EventSet::addEvent(Event* event)
{
	assert (0);
}

void EventSet::removeEvent(Event* event)
{
	assert (0);
}

Event* EventSet::waitSingle(int timeout)
{
	assert (0);
	return 0;
}

Event* EventSet::waitAll(int timeout)
{
	assert (0);
	return 0;
}

}
