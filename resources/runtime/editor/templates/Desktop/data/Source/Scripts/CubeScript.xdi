<?xml version="1.0" encoding="utf-8"?>
<object type="traktor.script.Script" version="2">
	<text>
	<![CDATA[
-- CubeScript
import(traktor)

CubeScript = CubeScript or class("CubeScript", traktor.world.ComponentEntity)

function CubeScript:new()
	self.angle = 0
end

function CubeScript:update(totalTime, deltaTime)
	self.angle = self.angle + deltaTime
	self:setTransform(Transform(
		Vector4.zero,
		Quaternion.fromEulerAngles(self.angle, self.angle * 0.66, self.angle * 0.33)
	))
end

function CubeScript:reset()
	self.angle = 0
end

	]]>
	</text>
</object>
