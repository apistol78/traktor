<?xml version="1.0" encoding="UTF-8"?>
<Workspace version = "1.0">
<?--
	
for _, project in pairs(solution.projects) do
	if project.enable then
		output:printLn("\t<FileRef location=\"group:" .. project.name .. ".xcodeproj\"/>")
	end
end

--?></Workspace>
